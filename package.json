{
  "type": "module",
  "private": true,
  "scripts": {
    "format": "prettier --write --list-different .",
    "lint": "prettier --check ."
  },
  "dependencies": {
    "prettier": "^3.6.2",
    "prettier-plugin-blade": "^2.1.21"
  }
}
